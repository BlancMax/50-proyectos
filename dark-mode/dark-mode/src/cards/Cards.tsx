import { Button } from './Button';
import CardContent from './cardContent';

const cards = () => {
    return (
        <div>
            <CardContent />
            <Button>More</Button>
        </div>
    );
};

export default cards;