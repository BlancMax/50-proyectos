import styled from 'styled-components';

export const Button = styled.button`
    /* background: tomato;
    font-size: 1em;
    margin: 1em;
    border: 2px solid black;
    /* border-radius: 8px; */
    border: 1px solid transparent;
    border-radius: 8px; 
        padding: 0.6em 1.2em;
        font-size: 1em;
        font-weight: 500;
        font-family: inherit;
        background-color: #da46e8;
        cursor: pointer;
        transition: border-color 0.25s;
        

    &:hover {
        border-color: #646cff;
      }
    &:focus,
    &:focus-visible {
        outline: 4px auto -webkit-focus-ring-color;
      }

`;

